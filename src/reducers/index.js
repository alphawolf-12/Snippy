import reducer from './reducer';

/**
 * In case of multiple reducers this file 
 * serves the purpose of combining them 
 * since we have a single reducer we just export it
 */

export default reducer;
